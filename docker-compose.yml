
services:

  mysql:
    image: mysql:latest
    container_name: mysql_kth
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: user
      MYSQL_PASSWORD: user
    volumes:
      - db_data_mysql:/var/lib/mysql
    ports:
      - '${MYSQL_PORT}:3306'

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin_kth
    environment:
      PMA_HOST: mysql
      MYSQL_ROOT_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "8082:80"
    depends_on:
      - mysql


volumes:
  db_data_mysql: